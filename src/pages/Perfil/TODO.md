# TODO - Logout no Perfil

- [x] Adicionar botão **Logout** em `Perfil.jsx`.

- [x] Implementar `handleLogout()` removendo `localStorage.usuarioLogado` e redirecionando para `/login`.

- [x] Adicionar estilos do botão em `Perfil.css` seguindo a paleta do site (roxos/transparências).

- [x] Validar build/execução do front (npm scripts do `projetoweb`).


